--- cpu_task.py (before)
+++ cpu_task.py (after)
@@ -4,7 +4,10 @@
 def fib(n: int) -> int:
     if n <= 1:
         return n
-    return fib(n-1) + fib(n-2)
+    a, b = 0, 1
+    for _ in range(2, n + 1):
+        a, b = b, a + b
+    return b
 
 # Return value type: dict
 def busy_cpu_task():
